# Строка 1: Импортируем библиотеку для интернета
import requests

# Строка 2: Пробуем подключиться к серверам Groq
try:
    # Строка 3: Отправляем простой запрос к Groq
    response = requests.get("https://api.groq.com", timeout=5)
    # Строка 4: Если подключились - говорим что всё ок
    print("✅ Соединение с Groq API есть!")
except:
    # Строка 5: Если не смогли подключиться - говорим об ошибке
    print("❌ Нет соединения с Groq API!")